import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    flexHorizontal: {
        flex: 1,
        flexDirection: 'row',
    },
    center: {
        alignItems: 'center',
        justifyContent: 'center',
    },
    flexGrow: {
        flexGrow: 1,
    },
});
