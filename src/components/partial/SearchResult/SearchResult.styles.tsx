import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    result: {
        flexDirection: 'row',
        marginBottom: 10,
    },
    icon: {
        marginRight: 7,
    },
});
